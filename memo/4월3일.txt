1교시
1)tensorflow 3번문제

개고양이
말사람 (sigmoid, categorical) -> 원핫한다음 카테고리컬하면됨.
가위바위보
중에 하나나옴.

target_size로 동일하게 사진을 나눔.

test 데이터를 증폭시키거나, 건드리지 않는다.
단, size크기와 모델평가를 위한 와꾸는 맞춰준다.

54_1에서 print(xy_train.shape) #error에서 오류뜬이유
list이기때문에 모양이 안나옴.

warning의 뜻 -> 돌아가기는 하는데 하지말라고 권유


2교시
1)f1 score 
4번 검증.
모터 진동과 모터 전동을 더한다.(컬럼을 합친다.)

".next()" 메서드는 ImageDataGenerator 클래스를 사용하여 생성된 증강 데이터 생성기에서 데이터의 다음 배치를 가져오는 데 사용됩니다. 
이 생성기는 뒤집기, 이동 및 회전과 같은 다양한 변환을 적용하여 원본 데이터의 새 버전을 생성합니다. 
생성기는 ".next()" 메서드를 호출하여 원래 훈련 세트에 있는 첫 번째 이미지의 증강 버전으로 구성된 데이터의 다음 배치를 생성합니다. 
결과 데이터는 증강 이미지와 해당 레이블을 모두 포함하는 변수 "x_data"에 저장됩니다. 
".next()" 메서드를 사용하는 이유는 데이터를 한 번에 모두 생성하는 것이 아니라 즉석에서 생성할 수 있기 때문에 많은 양의 메모리가 필요합니다.

요약하자면 .next() 앞에있는 한덩어리를 그대로 가져오겠다.